#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a[] = { 0, 1, 2, 3, 4, 5,};
    long int somavet(int *a, int qtde);
    int resultado;


    int fatorial( int x);
    {
        if (a < 7, resultado = (somavet, 6) i++ );
        printf("o resultado da soma é: %ld");

    }
}